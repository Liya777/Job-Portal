import { createTheme } from "@mui/material";

const palette = createTheme({
      palette: {
    common: {
        black: '#000',
        white: '#fff',
      },
      primary:{
        main:'#1f2831'
      },
      secondary:{
        main:'#ed563b'
      }
  }});

export default palette;
