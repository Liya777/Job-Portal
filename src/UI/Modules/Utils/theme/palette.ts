import { createTheme } from "@mui/material";

const palette = createTheme({
      palette: {
    common: {
        black: '#000',
        white: '#fff',
      },
      primary:{
        main:'#2c3c4c'
      },
      secondary:{
        main:'#ed563b'
      }
  }});

export default palette;
